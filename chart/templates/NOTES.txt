cyrostack-coder has been installed.

Coder URL:
  {{ .Values.domain.scheme }}://{{ .Values.domain.host }}

Wildcard workspace URL:
  {{ .Values.domain.wildcardHost }}

Workspace namespace:
  {{ .Values.workspace.namespace }}

Workspace ServiceAccount:
  {{ .Values.workspace.rbac.serviceAccountName }}

Expected external secrets:
  - coder-postgresql-secret
  - coder-db-url

Example database URL:
  postgres://coder:<password>@coder-postgresql.coder.svc.cluster.local:5432/coder?sslmode=disable

After Coder is running, push the workspace template:

  coder login {{ .Values.domain.scheme }}://{{ .Values.domain.host }}
  cd templates/cyrostack-devops
  coder templates push cyrostack-devops
